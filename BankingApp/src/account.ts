export class Account {
    accountNo ?: number;
    accountHolder ?: string;
    branch ?: string;
    bankCode ?: number;
    aType ?: string;
    accountBalance ?: number;

    constructor(
    accountNo : number,
    accountHolder : string,
    branch : string,
    bankCode : number,
    aType: string,
    accountBalance: number
    ){
    }   
}